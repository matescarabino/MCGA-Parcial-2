import React from 'react'
import styles from './form.module.css';

const Products = () => {
  return (
    <section className={styles.container}>
        <h2>Form - Se accedera con botones desde Products</h2>
    </section>
  )
};

export default Products;